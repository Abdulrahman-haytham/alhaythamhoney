# Copy to .env.production. Never commit real secrets.
POSTGRES_USER=alhaytham
POSTGRES_DB=alhaytham
# Use hex: openssl rand -hex 32 (avoids URL-encoding issues in DATABASE_URL).
POSTGRES_PASSWORD=CHANGE_ME
ADMIN_SESSION_SECRET=CHANGE_ME
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGE_ME
ADMIN_NAME=عبدالرحمن
NEXT_PUBLIC_SITE_URL=https://alhaythamhoney.sy

# Required for customer login codes. Use your SMTP provider; URL-encode credentials.
SMTP_URL=
MAIL_FROM="الهيثم — نحل وعسل <no-reply@alhaythamhoney.sy>"
# Generate a separate secret with: openssl rand -hex 32
CRON_SECRET=CHANGE_ME
